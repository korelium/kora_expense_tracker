enum TransactionType { income, expense }

enum AccountType { asset, liability }

enum AccountSubType {
  // Assets
  bank,
  cash,
  digitalWallet,
  investment,

  // Liabilities
  creditCard,
  loan,
  debt,
}

enum CategoryType { income, expense }
